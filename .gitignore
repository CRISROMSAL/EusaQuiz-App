# Node
node_modules/
# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env files (no subir ficheros con secretos)
.env
.env.local
.env.*.local
# Ej: .env.production, .env.development.local -> evitar subirlos
# (si quieres mantener .env.example, NO lo ignores)

# Build / output
dist/
build/
coverage/
.nyc_output/
.cache/
.sourcemaps

# OS / Editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# Process files / runtime
pids/
*.pid
*.seed
*.pid.lock
*.tgz

# Logs folder if you have one
logs/
*.log

# Database files
*.sqlite
*.sqlite3

# Uploads / temporales
uploads/
tmp/
temp/

# Tooling
.eslintcache
.parcel-cache/
.rpt2_cache/
.cache

# Optional: lockfiles *should be tracked* (package-lock.json, yarn.lock) — no los ignores
# If you use pnpm, keep pnpm-lock.yaml tracked

# Secrets / keys
*.pem
*.key
*.crt
